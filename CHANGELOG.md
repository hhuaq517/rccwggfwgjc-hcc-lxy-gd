# 变更记录

## v1.1.0 · 2026-09-20 · 公开版（脱敏）

仓库由私有转为公开。功能未变，**只做数据脱敏与配套的可用性处理**：

| 项 | 改前 | 改后 |
|---|---|---|
| `font-license-audit/assets/licensed-fonts.json` | 单位真实的 30 款已购字体清单（含授权主体 / 期限 / 内部备注 / 1 条排除记录） | **示例数据**：3 条占位条目（1 条开源字体 + 2 条 `示例字体X（请替换）`）+ 1 条排除范例；`_字段说明` 等填写说明完整保留 |
| `licensed-fonts.example.json` | 与正式清单并列的空白模板 | **删除**（正式清单本身已成模板，两个文件角色重复会让使用者不知道改哪个） |
| `check_fonts.py` | —— | 新增**模板检测**：清单还是示例数据时先打一段醒目提示，说明"下面的缺失条目是占位符，不用去找、也装不上"，并给出替换步骤。避免有人照着 `示例字体甲` 去装字体 |
| 内部人名（`font-license-audit/SKILL.md` ×5、`audit.py` ×2、`font_lookup.py` ×1、`ignored-terms.md` ×1、`安装说明.md` ×1） | 记录为具体对接人姓名 | 统一改为「**物料所属公司**」/「技能维护人」——保留"口径由谁在何时定下"的溯源价值，不暴露个人信息 |
| `LICENSE` / `README` / `安装说明.md` / `font-license-audit/SKILL.md` | 描述"内部清单、不得外发" | 同步为"仓内为示例数据、用前须替换"，并补上替换步骤与授权书口径说明 |

> **本次未脱敏的内容**（属审查方法本身，且素材本就为对外投放的公开物料）：
> 7 个实战案例中的班型名、价格与退费金额；`compliance-rules.md` 的口径表述与出处说明。
> 这些是技能的教学价值所在，若要一并占位化请另行说明。

## v1.0.0 · 2026-09-20 · 首发

把「宣传物料合规审查」这套技能包整理成一个仓库发布，含**两条独立的风险线**。

### 新增能力

**`ad-compliance-check` —— 广告法违禁词审查**

- 按风险等级标注：红（绝对化用语 / 保证性承诺 / 内部明令禁用词）、橙（赠品未明示 /
  价格无基准 / 数据未标来源 / 限时可能失实）、黄（口径不一致 / 疑似错别字）
- **每判定一处违规，当场给 2–3 个可直接抄进图里的替代写法**，不允许只指出问题
- 输入形态覆盖四类：位图（JPG/PNG）、PSD 源文件、文字稿 / 设计需求单（Excel 等）、
  用户指定的内部合规文件（优先级高于内置清单）
- 内置三份可维护的清单：`compliance-rules.md`（红线）、`replacement-words.md`（替代词对照）、
  `ignored-terms.md`（忽略清单，**优先级最高**，命中的词不标框、不给替代词、不凑数）
- 位图拿坐标用**像素墨迹投影 + 表格边框线检测**，取代"目测估比例"
- PSD 输入时直接读图层文案与 `bbox`，并能识别**隐藏层 / 被遮挡层**造成的"幽灵框"

**`font-license-audit` —— 字体版权比对**

- 三层流程：源文件元数据提取（确定性）→ 字形比对打分（量化排名）→ 付费接口（兜底）
- 四档结论：✅ 在清单内且在授权范围 / ⚠️ 已购但需核授权书 / ⛔ 已知排除（禁用） /
  🚫 当前清单中未见
- 源文件支持 PSD / AI / PDF / PPTX / DOCX / XLSX，按**文件魔数**判定格式，不看扩展名
- PSD 分支按 `RunArray` + `RunLengthArray` **按位对齐**，只报真正分到字符的字体
  （不是「FontSet 里出现过」就报），并可**拆解智能对象**内嵌的 PSB / AI / PDF
- 候选池自检：已购字体解析率低于门槛时**拒跑（退出码 3）**，而不是给一份"矮子里拔将军"的排名

### 工程约定

- 脚本之间**互相解耦**，可单独跑也可串起来；除 PSD 智能对象拆解外全部只用标准库
- 路径一律相对脚本目录解析，字体按**文件名**在系统字体目录里查找，不写机器绝对路径；
  追加目录走 `--font-dir` 或环境变量 `FONT_LICENSE_AUDIT_FONT_DIRS`
- 坐标统一用 **0–1 比例**，与图片分辨率无关
- 「一个编号 = 一条问题」：同一条问题在图上多处出现时共用编号，
  保证**图上编号与结论文档的条目数始终相等**
- 两条线的"文字框位"信息可互相复用，同一份物料不必重新定位

### 相对原始提交版的调整（可移植性修复）

原包在 Windows 上开发，有两处会让 macOS / Linux 使用者**拿到错结果而不自知**的静默降级，
本次发布前修掉：

| 文件 | 改前 | 改后 |
|---|---|---|
| `ad-compliance-check/scripts/annotate.py` | 编号/图例字体写死 `C:/Windows/Fonts/arialbd.ttf` 与 `msyh.ttc`，找不到就被 `except` 吞掉、回退 Pillow 默认位图字体 | 改为**跨平台候选表**（Windows → macOS `Arial Bold.ttf` / `STHeiti Medium.ttc` → Linux `DejaVuSans-Bold.ttf` / `Noto Sans CJK`）逐个探测；全部失败才回退，并在 stderr 显式告警 |
| `font-license-audit/scripts/glyph_match.py` | 未购对照字体只列了 Windows 自带的 6 款（黑体/微软雅黑/等线/宋体/楷体），非 Windows 平台上对照池为空，**脚本仍照常出排名** | 对照池补入 macOS（华文黑体/华文细黑/华文宋体/冬青黑体简）与 Linux（Noto Sans CJK/文泉驿正黑）候选；对照池为空时在报告开头**显式告警**，说明"未购字体冲上第一"这个信号本次无法发现 |

| `ad-compliance-check/SKILL.md` | 描述"Windows 下字体自动取 `C:/Windows/Fonts/...`，缺失时回退默认字体" | 同步为跨平台候选表 + stderr 告警的说法（文档与代码必须对得上） |

> 渲染逻辑一行未动，只调整了**字体路径解析与候选池组成**。
> 改动共 **3 个文件**（2 个脚本 + 1 处文档同步），其余 **16 个文件**与原提交版**逐字节一致**
> （已用 MD5 逐一比对确认）。
> 若你需要严格原样发布，说一声即可改回去。
>
> （本节描述的是 v1.0.0 的状态；v1.1.0 的公开版又在此基础上改了 5 个文件，见文首。）

### 已知约束

- **不附带任何字体文件。** 字体版权比对依赖本机已安装的已购字体（第 2 层），
  装到哪个目录都行，但要先跑一次 `check_fonts.py` 确认覆盖率达标。
- **只有成图时给的是排除性结论。** 字形比对只能排除多数未购字体、缩小范围，
  不能反证"就是某一款字"；确证性结论必须靠源文件（PSD/AI/PDF/PPTX/DOCX/XLSX）。
- **栅格化的大标题是共同盲区。** 做了描边/渐变/立体特效后合并的标题在源文件里
  已不是文字层，广告法侧画框要靠墨迹/颜色掩码反查位置，字体侧无法定款 ——
  报告里必须单列，不能默认它没问题。
- **本体不含 `package.py` 等打包脚本**，本仓库即分发包。
- 审查范围默认**只审当前点名的那一个文件**，这是刻意的设计，不是能力限制。
