# 宣传物料合规审查 · 安装说明

这个包里装着两个技能，是一套的：

| 技能 | 管什么 |
|---|---|
| `ad-compliance-check` | 广告法违禁词审查，在原图上标红橙黄框，逐条给替代词 |
| `font-license-audit` | 字体版权比对，出「在清单内 / 需核授权书 / 已知排除 / 清单中未见」四档结论 |

审一套物料时**两条线都要跑**：文案违禁和字体侵权是同一张图上的两个独立风险，只查一条会漏另一条。

---

## 一、安装

把包里的两个目录**整个**复制到技能目录下：

- Windows：`C:\Users\<你的用户名>\.workbuddy\skills\`
- macOS / Linux：`~/.workbuddy/skills/`

复制完应该长这样：

```
.workbuddy/skills/
├── ad-compliance-check/
│   └── SKILL.md
└── font-license-audit/
    └── SKILL.md
```

放着不管就行，不用注册。**目录名必须保持原样**——技能的识别靠这个名字，改了就不认。

## 二、装 Python 依赖

技能里的脚本要用到三个包：

```bash
pip install Pillow numpy psd-tools
```

| 包 | 用途 | 是否必须 |
|---|---|---|
| `Pillow` | 图片读取、标注、字形渲染 | **必须**，两个技能都要 |
| `numpy` | 框位计算 | 必须（广告法那侧） |
| `psd-tools` | 解析 PSD 图层；拆解 PSD 里的**智能对象**（内嵌 logo、素材图里的字体） | 可选，但**审 PSD 时建议装** |

少装 `Pillow` 的话，一跑脚本就会报 `ModuleNotFoundError: No module named 'PIL'`。

没装 `psd-tools` 不影响主线：PSD 的逐层字体分析照常跑，只是智能对象内部的字体读不到，
脚本会在备注里写明（不会静默跳过），装上后重跑即可。

## 三、字体（只影响字体那个技能）

字体比对靠的是「拿已购字体渲染出字，跟图上的字比形状」，所以**本机得装着对应字体**。

装完先体检一次：

```bash
python font-license-audit/scripts/check_fonts.py font-license-audit/assets/licensed-fonts.json
```

它会告诉你清单里的字体装上了几款、还缺哪几款。

**字体装到哪个目录都行**，脚本会自动在系统字体目录里按文件名找，不需要你手工指路。

如果装得太少（已购字体命中率低于 60%），脚本会**拒绝出排名**而不是勉强给一个。看到拒跑提示，说明本机字体不全，先把字体装上再跑——这是故意的设计，避免拿到一份看着正常其实错的结论。

## 四、怎么用

把图片或 PSD 直接丢给 AI 就行。它会先问你这次要审哪一项：

> 广告法合规审核 / 版权字体审查 / 两者都要

选完才开跑，不会默认开审。同一份物料两个都要的话，选第三项一次跑完。

也可以直接说「帮我审一下这张图，看有没有违禁词」，它就跳过询问直接走广告法那条线。

### 一次只审你发的那一个文件

**发一张就审一张**，这是刻意的设计，不是能力限制：

- 不会去列你电脑上的目录，也不会把同一文件夹里别的图一起审掉
- 文件名看着像一套的（`xxx-v1.png` / `xxx-v2.png`）也不会自动带上
- 交付物只对应你发的那一个文件，文件名一一对应，不会串

如果你确实想一次审多张，在消息里明说就行，例如「这个文件夹里的图都看一遍」——
这时它会**先把要审的文件清单列出来让你确认**，再动手，而且每张图分开出结论。

## 五、包里的《已购字体清单》

`font-license-audit/assets/licensed-fonts.json` **仓内自带的是示例数据**（3 条占位条目 + 1 条排除示例），
不是任何单位的真实清单 —— **第一次使用前必须整体替换成本单位自己的清单**，否则字体比对形同虚设：

```bash
# 先把本机装了的字体读出来（名称取自字体文件的 name 表，不要靠记忆猜）
python font-license-audit/scripts/font_info.py ~/Library/Fonts --json fonts.json
```

文件里的 `_字段说明` 逐字段写了怎么填，照着改即可。填完跑一次体检确认：

```bash
python font-license-audit/scripts/check_fonts.py font-license-audit/assets/licensed-fonts.json
```

> 清单只记录「买了哪些、能用在哪」，**不构成授权凭证** —— 最终授权结论以字体权利方
> 出具的授权书为准。本仓库**不附带任何字体文件**：字体的安装需自行完成，
> 且随包分发已购字体可能超出授权范围。

---

有问题找技能维护人。
